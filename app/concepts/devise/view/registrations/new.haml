/ TODO: Check js
-#= content_for :javascript do
-#  = javascript_include_tag 'user-forms'

.group-form
  = form_for(model, as: :user, url: user_registration_path, html: { role: 'form', class: 'user-form' }) do |f|
    / TODO: Add missed fields
    -#= f.text_field :name, autofocus: true, placeholder: "Ім'я*", class: 'custom-input'
    = f.email_field :email, autofocus: true, placeholder: t('activerecord.attributes.user.email'), class: 'custom-input'
    = f.password_field :password, placeholder: t('activerecord.attributes.user.password'), class: 'with-error custom-input'
    = f.password_field :password_confirmation, placeholder: t('activerecord.attributes.user.password_confirmation'), class: 'custom-input'
    -#= f.text_field :settlement, data: { 'autocomplete-url' => search_settlements_users_url(limit: 10, query: '_QUERY_'),
    -#               'autocomplete-search' => 'settlement' }, placeholder: 'Оберіть населений пункт', class: 'autocomplete custom-input'
    -#= f.hidden_field :settlement_type, class: 'settlement-type custom-input'
    -#= f.text_field :region, disabled: true, class: 'region hidden custom-input'
    -#= f.hidden_field :district, class: 'district custom-input'
    = f.submit t('views.titles.devise.sign_up'), class: 'button', data: data_disable_with
  = cell(::Devise::Cell::Links)
