%h1.title= link_to decision_request.title, title_url_resource
.info
  %p.author= link_to_if policy?(:show, user), user.full_name, user
  %p.date= l(created_at, format: '%d %B, %Y')
%br
= description_block

