.decision{ id: "decision-#{id}", class: decision_class }
  .row
    .col-md-4
      .photo
        = link_to image_tag(user_avatar.url), model
    .col-md-8
      = cell(::Decision::Cell::Info, model, list: true)
      = cell(::Decision::Cell::Categories, model)
